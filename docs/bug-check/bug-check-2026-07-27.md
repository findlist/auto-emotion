# Bug 检查与修复报告 - auto-emotion

## 检查时间
2026-07-27 02:20

## 检查范围
- 前端（client 目录）：
  - `npm run lint`：通过（EXIT 0）
  - `npm run test`：通过（Vitest，单线程模式 `npx vitest run --no-file-parallelism`）
  - `npm run build`：通过（Vite 构建）
  - 代码质量扫描目录：`src/pages`、`src/components`、`src/game`、`src/stores`、`src/utils`
- 后端（server 目录）：
  - `npm run test`：通过（Vitest）
  - 代码质量扫描目录：`src/routes`、`src/services`、`src/middleware`、`src/websocket`
- 代码变更分析范围：`git diff HEAD~1`，重点覆盖游戏引擎/实体/场景、WebSocket 连接与房间管理、类型与异常处理

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P0 | server/src/middleware/rate-limit.ts | 滑动窗口限流 `zadd` 的 member 使用 `Math.random()` 生成，同毫秒内碰撞时会被 Redis 覆盖，导致窗口内实际计数被低估，限流功能在高并发下失效，安全风险高 | 改用 `randomUUID()` 生成唯一 member，避免碰撞造成计数丢失 | f5277a3 |
| 2 | P1 | client/src/game/games/boss-game.ts | Boss 被击败时仅 `removeChild` 移除显示对象，未调用 `destroy({ children: true })`，导致 Graphics 及其子节点（hpBarBg/hpBar）的 GPU 资源泄漏，长时间游玩或重复对战后显存持续增长 | 在 `removeChild` 之后显式 `this.boss.sprite.destroy({ children: true })` 再置空引用 | ae92a35 |
| 3 | P1 | client/src/game/games/brawl-game.ts | `shootToward` 未校验玩家 `alive` 状态，死亡玩家仍可触发本地射击，进而触发 `onLocalShoot` 回调将"幽灵射击"事件上报后端并广播，造成无效网络流量与游戏逻辑错乱 | 在 `shootToward` 入口处补 `if (!data \|\| !data.alive) return;` 守卫，与 `shoot` 保持一致 | ae92a35 |
| 4 | P1 | server/src/services/settle-service.ts | 结算服务在排序过程中直接修改入参 `players`（原地 `sort` 与 `isMvp` 赋值），若调用方依赖入参不可变性会引发隐性 bug，且 MVP 标记可能污染上游状态 | 改用 `[...players].sort(...).map((p, i) => ({ ...p, isMvp: i === 0 }))` 返回新数组，避免修改入参 | f5277a3 |
| 5 | P1 | server/src/websocket/handlers.ts | `handleDisconnect` 中遍历 `socket.rooms` 缺少外层异常保护，异常状态（如 rooms 不可迭代）下会抛出未捕获异常，影响断线流程；内部 `emit` 也会因底层传输关闭而抛错中断后续房间广播 | 外层加 `try/catch` 保护遍历，内部 `emit` 单独 `try/catch` 并记录日志，保证单房间失败不影响其他房间广播 | f5277a3 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| - | - | - | 本次扫描未发现需要大范围改动或可能引入新风险的 P0/P1 问题 | - |

## 记录的轻微问题（P2，未修复）

- 部分游戏文件中参数较多，可考虑后续聚合为配置对象（不影响功能）。
- 部分工具函数缺少详细 JSDoc，但不影响公共 API 使用（项目内部工具）。
- 部分日志为英文，与项目中文风格略有差异，可后续统一（不影响功能）。
- `brawl-game.ts` 中 `BOUNCE_DAMPING` 等常量与玩家半径等数值配置可后续抽到独立配置文件统一管理（当前集中维护已可读）。

> 以上 P2 问题仅记录，未修改任何代码，遵循"只修复 P0/P1"约束。

## 验证结果

- 修复后 lint（client）：通过
- 修复后 test（client）：通过
- 修复后 build（client）：通过
- 修复后 test（server）：通过

### 提交记录

- client 仓库：`ae92a35` — `fix: 修复 boss 被击败时 sprite 未销毁导致 GPU 资源泄漏问题`（已 push 至 `origin/HEAD`）
- server 仓库：`f5277a3` — `fix: 修复限流 member 碰撞、断线遍历异常逃逸与结算入参副作用问题`（已 push 至 `origin/HEAD`）

### 执行约束符合性

- ✅ 高权限自主执行，未等待人工确认
- ✅ 仅修复 P0/P1 问题，P2 仅记录
- ✅ 每次修复仅改动最小必要代码，未做重构、未新增功能
- ✅ 所有修复通过 build/test 验证
- ✅ `git add` 仅添加本次修改文件，未使用 `git add -A`
- ✅ 未使用 `force push`、`reset --hard` 等破坏性命令
- ✅ 今日（2026-07-27）此前不存在同名报告，本次为首次生成，未覆盖任何已有报告
