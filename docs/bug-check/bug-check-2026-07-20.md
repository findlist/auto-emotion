# Bug 检查与修复报告 - auto-emotion

## 检查时间
2026-07-20 00:25

## 检查范围

### 前端（client 目录）
- `npm run lint`：通过（exit 0，无 ESLint 错误）
- `npm run test`：通过（253 个测试全部通过，31 个测试文件）
- `npm run build`：通过（39.76s 完成，产物含 idle/shop/home/room 等页面 chunks）
- 检查目录：src/pages、src/components、src/game、src/stores、src/utils、src/hooks

### 后端（server 目录）
- `npm run test`：通过（714 个测试全部通过，56 个测试文件，含本次新增 deductGold/getUserGold 工具测试 3 个）
- `npm run build`：通过（tsc 无类型错误）
- 检查目录：src/routes、src/services、src/middleware、src/websocket、src/idle、src/utils、src/config

### 代码变更分析范围
- `git diff HEAD~1`：HEAD 提交（572d30b）为 refactor: room-manager 兜底数据字面量提取为常量（FALLBACK_STRESS_SOURCE / FALLBACK_MONSTER_NAME），仅提取常量无可执行逻辑变化
- 工作目录未提交变更（其他自动化流程并行工作）：16 个文件改动，重点审查：
  - 后端：`server/src/utils/gold.ts`（新增金币工具）、`pet-service.ts`、`skill-service.ts`、`weapon-service.ts`、`gold.test.ts`
  - 前端：`client/src/pages/idle.tsx`（+121 行）、`shop.tsx`、`home.tsx`、`room.tsx`、`achievements.tsx`、`tasks.tsx`、`index.css`
  - WebSocket：`server/src/websocket/room-manager.ts`（已含常量提取）

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| - | - | - | 本次检查未发现新的 P0/P1 问题需要修复 | - | - |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| - | - | - | 本次未发现需评估的新问题 | - |

## 记录的轻微问题（P2，未修复）

- `server/src/services/match-service.ts`：matchTimers 类型注解可改为 `Map<string, NodeJS.Timeout>` 提升类型安全性（延续自昨日，未变化）
- `server/src/services/season-pass-service.ts`：claimSeasonReward 中 `rewardId = isPremium ? reward.premium_reward_id : 0`，free 分支硬编码 0 与原 free_reward_id 字段不一致，但因 SEASON_REWARDS 中 free_reward_id 始终为 0 不影响行为（延续自昨日，未变化）
- `server/src/services/idle/idle-engine.ts`：settle 中 `Math.random() < FRAGMENT_DROP_RATE` 不可重放，单元测试无法覆盖掉落分支（延续自昨日，未变化）
- `client/src/game/games/boss-game.ts`：useUltimate 直接对 boss.hp 减 200 未走 damage 抽象，未来扩展元素抗性时需重构（延续自昨日，未变化）
- `client/src/pages/lobby.tsx`：handleJoinRoom 的 catch 分支未区分超时与拒绝连接，可细分错误码（延续自昨日，未变化）

## 验证结果

### 修复前（即检查时现状）
- client lint：通过
- client test：通过（253 个测试）
- client build：通过（39.76s）
- server test：通过（714 个测试，含 3 个新增 deductGold/getUserGold 用例）
- server build：通过（tsc 无错误）

### 修复后
- 本次未做修复，验证结果同上

## 检查结论

### 工作目录未提交变更审查结论

后端核心改动（金币扣减工具抽取）质量良好：
- `server/src/utils/gold.ts`：抽取 `getUserGold`/`deductGold` 公共工具，统一 4 处 service 重复模板
  - `getUserGold` 在用户不存在时统一抛 NOT_FOUND（与 weapon-service 既有契约一致），消除原 3 处 TypeError 500 隐患
  - `deductGold` 保留 SQL 原子守卫 `AND gold >= $1 RETURNING gold`，参数顺序 `[amount, userId]` 与 SQL 占位符 `$1/$2` 一致
  - 配套 3 个单元测试覆盖扣减成功/失败和参数顺序校验
- `pet-service.ts` / `skill-service.ts` / `weapon-service.ts`：均使用 `deductGold` + `getUserGold` 替代内联模板，事务隔离正确，无并发扣减使金币变负风险
- `shop-service.ts`：保留原内联扣款（错误码 BAD_REQUEST 与工具 FORBIDDEN 不一致，未强行统一避免破坏 shop.test.ts 断言契约）— 设计取舍合理

前端 UI 改动（neo-brutalism 风格增强）质量良好：
- `idle.tsx`：useCallback 保证引用稳定、a11y tablist 语义完整、空状态补全、二次确认防误触、showApiError 统一错误处理
- `shop.tsx`：请求序号守卫防止竞态、useCallback 稳定引用、嵌套 tablist 子级语义正确
- `home.tsx`：cancelled 守卫避免卸载后 setState、装饰性 emoji 用 aria-hidden 屏蔽、装饰卡移除 card-hover 避免伪交互误导
- `room.tsx`：useShallow 浅比较优化、onGameStart ref 避免无限 effect、ready-dot 视觉指示、role=alert 强制屏幕阅读器立即朗读
- `achievements.tsx` / `tasks.tsx`：useAsyncEffect cancelled 守卫、progressbar a11y、归档态不加流光避免动画干扰

### 已修复问题回归验证

昨日（2026-07-19）修复的 4 P0 + 11 P1 均未回归：
- 前端：use-async-effect ref 赋值移入 useEffect、StrictMode ref 置空、speed-game 重置 spawnTimer、boss/brawl-game isRunning 守卫、lobby waitForConnection 等待握手
- 后端：friend-service acceptFriendRequest 参数顺序、achievement-service 原子累加、room-manager 状态恢复广播、auth middleware config 统一、app trust proxy、gracefulShutdown 超时兜底、season-pass season_level 重算

## 提交记录

- 本次检查未做修复，无 git commit
- 工作目录中的未提交变更为其他自动化流程并行工作，不属于本任务范围

## 修复概览

- P0 严重问题：0 个
- P1 中等问题：0 个
- P2 轻微问题：5 个（延续自昨日，未变化）
- 需评估问题：0 个
- 验证结果：前端 lint/test(253)/build 通过，后端 test(714)/build 通过
