# Bug 检查与修复报告 - auto-emotion

## 检查时间
2026-07-19 00:30

## 检查范围

### 前端（client 目录）
- `npm run lint`：通过（修复后无 react-hooks/refs 错误）
- `npm run test`：通过（253 个测试全部通过）
- `npm run build`：通过（1.62s 完成）
- 检查目录：src/pages、src/components、src/game、src/stores、src/utils、src/hooks

### 后端（server 目录）
- `npm run test`：通过（711 个测试全部通过，含 2 个本次新增/调整的事务化用例）
- `npm run build`：通过（tsc 无类型错误）
- 检查目录：src/routes、src/services、src/middleware、src/websocket、src/idle、src/utils、src/config

### 代码变更分析范围
- 分析 `git log HEAD~3` 以来的代码变更，重点关注：
  - 游戏逻辑（game 目录下的引擎、实体、场景）
  - WebSocket 连接和房间管理问题
  - 类型错误、异常处理缺失、性能问题
  - 数据库事务边界与并发安全

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P0 | client/src/hooks/use-async-effect.ts | 在 render body 中执行 `effectRef.current = effect;` 等 4 行赋值，触发 react-hooks/refs 规则 4 处错误，导致 lint 失败构建不通过 | 将 4 个 ref 赋值移入 useEffect（无依赖数组）内，每次 commit 后同步更新，effect 内部读取的回调始终为最新版本 | 3e1ef27 |
| 2 | P0 | client/src/pages/battle.tsx | React 19 StrictMode 下 effect 执行 mount→cleanup→mount 序列，cleanup 中 destroy 了 battleSceneRef.current 但未置 null，第二次 mount 时 `if (battleSceneRef.current) return;` 跳过重建，导致游戏画面无法初始化 | cleanup 末尾显式置空 battleSceneRef/sceneManagerRef/assetsRef/engineRef/tickerCallbackRef 五个 ref | 3e1ef27 |
| 3 | P0 | server/src/services/friend-service.ts | acceptFriendRequest 中 INSERT 参数顺序为 `[fromUserId, toUserId]`，与原 pending 记录方向相同，触发 UNIQUE(user_id, friend_id) 约束的 ON CONFLICT DO NOTHING 被忽略，导致只建立单向好友关系，接收方在 getFriends 中查不到对方 | 改为反向 `[toUserId, fromUserId]` 建立反向好友记录，让接收方在 getFriends（f.user_id = 接收者）中能查到 | a3ead22 |
| 4 | P0 | server/src/services/achievement-service.ts | updateAchievementProgress 使用 SELECT→计算→UPDATE 非原子模式，并发请求读到相同 progress 后各自 +delta，后写覆盖先写导致进度丢失（同时 +1 应为 +2，实际只 +1） | 改用 withTransaction + advisoryXactLock 串行化同用户同类型并发；SQL 改为单条 INSERT...ON CONFLICT (user_id, achievement_id) DO UPDATE SET progress = progress + delta 原子累加；WHERE NOT is_completed 跳过已完成成就 | a3ead22 |
| 5 | P1 | client/src/game/games/speed-game.ts | switchMiniGame 中未重置 spawnTimer，切换瞬间残留旧值（可能已远超 1000ms）立即生成目标，破坏"每秒生成一个"节奏，玩家无反应时间 | 添加 `this.spawnTimer = 0;` 重置计时基准 | 3e1ef27 |
| 6 | P1 | client/src/game/games/speed-game.ts | onBubbleHit/onTapeTorn/onWatermelonSmash 仅 removeChild 不 destroy，PixiJS Sprite 与纹理资源未释放，长时间游玩累积 GPU 内存 | 添加 `bubble.destroy()/tape.destroy()/watermelon.destroy()` 显式释放 | 3e1ef27 |
| 7 | P1 | client/src/game/games/speed-game.ts | handlePointerDown 未判断 targets 是否为空，切换小游戏瞬间 targets 短暂为空时点击被误判为"未命中"清空 combo | 添加 `if (this.targets.length === 0) return;` 守卫 | 3e1ef27 |
| 8 | P1 | client/src/game/games/boss-game.ts | handlePointerDown 缺少 isRunning 守卫，游戏结算后 isRunning=false 时点击仍可射击：1) 投射物加入 projectiles 但 update 不再执行导致残留；2) shootToward 触发 onLocalShoot 把已结束游戏中的"射击"上报后端造成幽灵操作 | 添加 `if (!this.isRunning) return;` 状态守卫 | 3e1ef27 |
| 9 | P1 | client/src/game/games/brawl-game.ts | 同 boss-game，handlePointerDown 缺少 isRunning 守卫，结算后仍可射击造成幽灵操作 | 添加 `if (!this.isRunning) return;` 状态守卫 | 3e1ef27 |
| 10 | P1 | client/src/pages/lobby.tsx | handleJoinRoom 中 connect() 同步返回但 socket.id 未就绪，立即调用 joinRoom 后端拿到的 socketId 可能为空，导致房间状态错乱（玩家加入但 socketId 不匹配，无法接收广播） | 改为 async 并 await waitForConnection() 等待连接完成获取有效 socket.id 后再调用 joinRoom | 3e1ef27 |
| 11 | P1 | server/src/websocket/room-manager.ts | generateLevelAndEvents 失败后 catch 内恢复 room.status='ready' 但仅广播 RoomEvents.ERROR，未广播 RoomEvents.STATE，前端 UI 据此无法切换回准备界面，房间实质卡死在 generating 状态（准备/开始按钮永久禁用） | 在恢复 status 后广播 STATE 事件携带最新 room 数据，前端据 status=ready 切回准备界面允许重试 | a3ead22 |
| 12 | P1 | server/src/middleware/auth.ts | 直接读 `process.env.JWT_SECRET!`，与项目约定（config 模块统一管理环境变量）不一致；config 在启动时已 assertRequired 校验过非空，运行时再次读 process.env 是冗余且绕过了启动校验 | 改为 `import { config } from '../config/index.js'` 并使用 `config.jwtSecret` | a3ead22 |
| 13 | P1 | server/src/app.ts | 未设置 trust proxy，生产环境位于 Nginx/CDN 后时 req.ip 会取代理 IP 而非真实客户端 IP，导致限流中间件（依赖 req.ip 区分用户）将所有用户当作同一 IP，单用户限流失效或全员被误限 | `app.set('trust proxy', 'loopback, linklocal, uniquelocal')` 信任本机/内网代理 | a3ead22 |
| 14 | P1 | server/src/app.ts | gracefulShutdown 无超时兜底，httpServer.close 在 keep-alive 连接未释放时会卡住，进程超出 K8s 宽限期被 SIGKILL 强杀导致脏数据；无 shuttingDown 标记导致优雅关闭期间仍接收新请求；无 httpServer.on('error') 监听导致 EADDRINUSE 等错误冒泡到进程崩溃；无 unhandledRejection 监听 | 1) httpServer.close 加 10s 超时兜底强制继续关闭流程；2) 新增 shuttingDown 标记防 SIGTERM/SIGINT 重复触发；3) 注册 httpServer.on('error') 结构化日志；4) 注册 process.on('unhandledRejection') 兜底日志 | a3ead22 |
| 15 | P1 | server/src/services/season-pass-service.ts | addSeasonExp 仅 UPDATE season_exp 累加，未更新 season_level，导致用户经验持续增长但等级始终为 1，后续 claimSeasonReward 校验 `user.season_level < level` 永远抛"等级不足"，所有赛季奖励无法领取 | UPDATE 时同步重算 season_level = GREATEST(season_level, (season_exp + $1) / 100 + 1)，每级 100 exp 与 SEASON_REWARDS 表对齐；GREATEST 防止降级；INT/INT 整数除法直接得整级数 | a3ead22 |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 1 | P0 | server/src/routes/settle.ts | userId 未校验真实参与对局：虽然路由已校验 callerInPlayers 防止给任意账号发奖，但 service 层未从 Redis 获取真实房间数据校验所有 players 都是房间成员。攻击者可构造 fake roomId + 自己作为 caller 来 farm 奖励（game_records.room_id 唯一性检查不验证房间存在性） | 修复需在 service 层引入 redis 客户端依赖（当前 service 层无 redis 引用），或路由层引入 room-manager（循环依赖风险），且需要新增 redis mock 到 settle-service.test.ts 测试基础设施；属于跨层改动，超出"最小必要代码"修复范围，需评估后再决定方案 |
| 2 | P1 | server/src/services/task-service.ts | ensureDailyTasksExist 中 COUNT=0 检查 + 多条 INSERT 非原子，并发请求都通过检查后各自 INSERT 3 条记录，导致今日任务列表出现 6 条重复记录，前端展示异常、领取奖励数据混乱 | daily_tasks 表无 (code, date) UNIQUE 约束，ON CONFLICT DO NOTHING 无冲突目标退化为 no-op；真正修复需新增 migration 添加 UNIQUE 约束 + 改用事务化 advisory lock 模式，且会破坏现有 task-service.test.ts 的 mock 结构（queryMock→clientQueryMock）；属于 schema + 测试基础设施同步改动，需评估 |
| 3 | P1 | server/src/services/match-service.ts | matchTimers 是模块级 Map，多实例部署时每个进程持有独立的 matchTimers，玩家在实例 A 创建匹配后定时器仅在 A 执行，若连接迁移到实例 B 则无法触发匹配完成；同时进程崩溃后 matchTimers 内存丢失 | 修复需将 matchTimers 迁移到 Redis（SET+EXPIRE+SCAN 模式）或共享状态存储，并改造所有调用方；属于分布式架构改造，需评估后单独排期 |
| 4 | P1 | server/src/websocket/handlers.ts | handleDisconnect 不移除玩家，仅广播 PLAYER_OFFLINE 通知（保留 5 分钟重连窗口由 TTL 兜底清理）| 经分析为设计取舍：玩家短暂断线（网络抖动、切后台）后重连可恢复房间状态，强制移除会导致 UX 退化；TTL 5 分钟兜底已覆盖崩溃场景；不属于 bug |
| 5 | P1 | server/src/idle/idle-engine.ts | settle 中 `while (currentExp >= expForLevel(newLevel))` 循环未加上限 | 经分析：expForLevel(level) = 100*level^2 二次增长，每轮循环减去递增的阈值，对任何合理的 currentExp 值循环都会快速终止（不会无限循环）；当前实现数学正确，无需修复 |

## 记录的轻微问题（P2，未修复）

- server/src/services/match-service.ts：matchTimers 类型注解可改为 `Map<string, NodeJS.Timeout>` 提升类型安全性
- server/src/services/season-pass-service.ts：claimSeasonReward 中 `rewardId = isPremium ? reward.premium_reward_id : 0`，free 分支硬编码 0 与原 free_reward_id 字段不一致，但因 SEASON_REWARDS 中 free_reward_id 始终为 0 不影响行为
- server/src/services/idle/idle-engine.ts：settle 中 `Math.random() < FRAGMENT_DROP_RATE` 不可重放，单元测试无法覆盖掉落分支
- server/src/websocket/room-manager.ts：generateLevelAndEvents 内兜底数据使用字面量 '工作压力' 与 '压力怪兽'，可提取为常量
- client/src/game/games/boss-game.ts：useUltimate 直接对 boss.hp 减 200 未走 damage 抽象，未来扩展元素抗性时需重构
- client/src/pages/lobby.tsx：handleJoinRoom 的 catch 分支未区分超时与拒绝连接，可细分错误码

## 验证结果

### 修复前
- client lint：失败（4 个 react-hooks/refs 错误）
- client test：失败（use-async-effect 5 个用例受 ref 修改影响）
- client build：失败（lint 阻断构建）
- server test：通过（708 个测试）
- server build：通过

### 修复后
- client lint：通过
- client test：通过（253 个测试全部通过）
- client build：通过（1.62s 完成）
- server lint：通过（项目无 lint 脚本，由 tsc 兜底类型检查）
- server test：通过（711 个测试全部通过，新增 2 个事务化用例覆盖 updateAchievementProgress）
- server build：通过（tsc 无类型错误）

## 提交记录

- **client 修复**：3e1ef27 `fix: 修复前端 P0/P1 问题`（6 个文件，74 行新增 / 17 行删除）
- **server 修复**：a3ead22 `fix: 修复后端 P0/P1 问题`（8 个文件，156 行新增 / 93 行删除）
- 已 push 到 origin/main（3477781..a3ead22）

## 修复概览

- P0 严重问题：4 个，全部修复（前端 2 个 lint/StrictMode + 后端 2 个好友关系/成就并发）
- P1 中等问题：11 个，全部修复（前端 5 个游戏逻辑 + 后端 6 个房间状态/中间件/优雅关闭/赛季等级）
- P2 轻微问题：6 个，仅记录未修复
- 需评估问题：5 个，记录原因待人工决策
