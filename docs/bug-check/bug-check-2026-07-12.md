# Bug 检查与修复报告 - auto-emotion

## 检查时间
2026-07-12 00:20

## 检查范围

### 自动化检查结果

| 检查项 | 目录 | 命令 | 结果 |
|--------|------|------|------|
| 前端 Lint | client | `npm run lint` | 通过（0 错误，9 警告） |
| 前端单元测试 | client | `npm run test` | 通过（27 文件，225 用例） |
| 前端构建 | client | `npm run build` | 通过（1.18s） |
| 后端单元测试 | server | `npm run test` | 通过（50 文件，647 用例） |
| 后端构建 | server | `npm run build` | 通过（tsc 无错误） |

> 注：后端无 lint 脚本（package.json 未配置）。后端测试 stderr 中的错误日志为测试用例预期抛出，非真实失败。
> 前端 9 个警告均为 `react-hooks/set-state-in-effect`（P2 性能建议），与 07-11 报告一致。

### 静态代码审查范围

- **前端**：client/src/pages、client/src/components、client/src/game、client/src/stores、client/src/utils
- **后端**：server/src/routes、server/src/services、server/src/middleware、server/src/websocket
- **代码变更分析**：`git log HEAD~10..HEAD`（涵盖 bdb6fb3 room-manager 分布式锁、91da75a 幂等控制、546dab0 路由测试竞态修复等近期代码提交）

### 历史 P2 问题复查

07-11 报告中记录的 P2 问题本次复查发现大部分已由并行开发修复：

| 历史 P2 问题 | 当前状态 | 说明 |
|--------------|----------|------|
| boss-game.ts 投射物双重 destroy 风险 | 已修复 | `if (hitDestructible) continue` 跳过出界检测 |
| battle-scene.ts 异步 init 未 await | 已修复 | init 改为同步，syncPlayers 增加 currentMode 守卫 |
| speed-game.ts switchMiniGame 未清理 pendingTimers | 已修复 | pendingTimers Set + switchMiniGame/cleanup 统一 clearTimeout |
| websocket/index.ts waitForConnection reject 过早 | 已修复 | 仅 reconnect_failed/超时 reject，不监听 connect_error |
| App.tsx handleLogout 未捕获错误 | 已修复 | try/finally 包裹，确保 reload 执行 |
| brawl-game.ts 玩家碰撞重复处理 | 已修复 | `otherId <= id` 字典序守卫确保每对仅处理一次 |
| room-manager.ts generateRoomId 用 Math.random | 已修复 | 改用 crypto.randomBytes |
| event-generator.ts 洗牌算法分布有偏 | 待评估 | 未在本轮检查范围确认 |
| level-generator.ts AI 返回数据校验过弱 | 待评估 | 未在本轮检查范围确认 |
| settle-service.ts ROLLBACK 未防错 | 已修复 | try/catch 包裹 ROLLBACK，记录 rollbackErr 不掩盖原始错误 |
| match-service.ts timer 回调未 catch | 已修复 | setTimeout 回调内 try/catch |
| match-service.ts getMatchStatus 多算 1 | 已修复 | meInQueue 检查统一处理 |
| app.ts 优雅关闭未 await 连接池 | 已修复 | await pool.end() + await redis.quit() |
| websocket/index.ts CORS 全开 | 已修复 | 改用 config.corsOrigin 环境变量控制 |
| settle.ts/room.ts 错误处理统一问题（07-11 需评估） | 已修复 | AppError 按 ErrorCode 映射 HTTP 状态码，其余 500 |

---

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|------|----------|------|----------|----------|----------|
| 1 | P1 | server/src/websocket/room-manager.ts | `updateRoomStatus` 是唯一缺少 `withRoomLock` 保护的读-改-写方法。`handleFinish` 调用其切换 playing→settling 时，若并发的 `setReady`/`submitStress` 已通过 `withRoomLock` 修改玩家数据并写回，`updateRoomStatus` 的 `setex` 会用过期的内存副本覆盖这些修改，导致玩家 `isReady`/`stressSources` 丢失 | 用 `withRoomLock` 包裹读-改-写逻辑，与其他 5 个方法（joinRoom/leaveRoom/setReady/setMode/submitStress）保持一致 | 118cb4e |

---

## 未修复的问题（需评估）

无。本轮检查未发现需评估的 P0/P1 问题。

---

## 记录的轻微问题（P2，未修复）

### 前端

- **多个页面**：9 处 `react-hooks/set-state-in-effect` 警告（useEffect 中同步 setState），涉及 achievements.tsx、battle.tsx、friends.tsx、idle.tsx、leaderboard.tsx、records.tsx、season-pass.tsx、shop.tsx、tasks.tsx。属性能优化建议，非功能 bug，与 07-10/07-11 报告一致，本次不修复。

### 后端

- **room-manager.ts generateLevelAndEvents**：该方法做读-改-写（设置 levelData 与 status）但未使用 `withRoomLock`。因其由 `startGame` 持有 `room:lock:start` 锁调用，且为长时异步 AI 生成操作，加 `withRoomLock` 会阻塞 setReady 等操作数秒。当前设计在 generating 状态下 setReady 语义无意义，竞态影响可接受，建议后续评估。
- **auth.ts middleware**：`redis.get(blacklist:${token})` 未 try/catch，Redis 故障时抛非 AppError 导致 500。WebSocket 握手已降级放行，HTTP 侧选择 fail-closed（更安全），属设计决策非 bug。

---

## 验证结果

| 验证项 | 修复前 | 修复后 |
|--------|--------|--------|
| 前端 lint | 通过（0 错误，9 警告） | 未触及前端，状态不变 |
| 前端 test | 225 passed | 未触及前端，状态不变 |
| 前端 build | success | 未触及前端，状态不变 |
| 后端 test | 647 passed | 647 passed（50 文件） |
| 后端 build | success | success（tsc 无错误） |

---

## 检查结论

本次检查共发现 **1 个 P1 问题**并修复通过验证，无 P0 问题，无需评估问题。

### 修复亮点

1. **并发安全修复**（room-manager.ts）：补全了 `updateRoomStatus` 缺失的 `withRoomLock` 保护，消除了 `handleFinish` 与 `setReady`/`submitStress` 并发时的丢失更新风险。此前 07-11 报告已为 5 个 read-modify-write 方法加锁，本次补齐了最后一个遗漏方法，使房间状态机的并发保护完整一致。

### 整体质量评价

经过 07-10、07-11、07-12 三轮 bug 检查，项目质量已显著提升：
- 前端 lint/test/build 全部通过，9 个警告均为 P2 性能建议
- 后端 test/build 全部通过，647 个测试用例覆盖核心业务逻辑
- 07-11 报告中的 16 个 P2 问题大部分已由并行开发修复（13/16 已修复）
- 房间状态机并发保护已完整（createRoom/joinRoom/leaveRoom/setReady/setMode/submitStress/updateRoomStatus 均使用 withRoomLock 或专用锁）
- 资源泄漏问题已全面修复（PixiJS Graphics/Container/Texture、setTimeout 句柄、Socket.IO 监听器）
- 安全问题已修复（JWT 异常处理、refreshToken 黑名单、CORS 收紧、docker-compose 端口绑定）
