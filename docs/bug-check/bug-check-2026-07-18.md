# Bug 检查与修复报告 - auto-emotion

## 检查时间
2026-07-18 00:40

## 检查范围

### 前端（client 目录）
- `npm run lint`：通过
- `npm run test`：通过（243/243）
- `npm run build`：通过
- 代码质量检查范围：src/pages、src/components、src/game、src/stores、src/utils

### 后端（server 目录）
- `npm run test`：通过（678/678）
- `npm run build`（tsc）：通过
- `npx tsc --noEmit`：通过
- 代码质量检查范围：src/routes、src/services、src/middleware、src/websocket

### 代码变更分析
- 分析 `git diff HEAD~1`，重点关注新增代码、游戏逻辑、WebSocket 连接与房间管理、类型错误、异常处理、性能问题
- 数据库 schema 验证：`database/migrations/001_init.sql` 确认 users.id / friendships.id 为 UUID 类型，证实 P0-1/P0-2 为真实 bug

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| P0-1 | 严重 | server/src/services/leaderboard-service.ts | getFriendsUserRank(L155)/getFriendsLeaderboard(L199) 中 `parseInt(UUID)` 截断 UUID 字符串，导致好友榜单查询 100% 失败 | LeaderboardEntry.userId 从 number 改为 string，移除 parseInt 直接 push userId | 2359e97 |
| P0-2 | 严重 | server/src/services/friend-service.ts, server/src/routes/friends.ts | friend-service 类型契约与 DB UUID 不匹配，routes/friends.ts 的 parseIdParam 截断 UUID，删除好友等接口 100% 失败 | PendingRequestRow.id 改为 string，sendFriendRequest/acceptFriendRequest/rejectFriendRequest/removeFriend 参数改为 string；routes 删除 parseIdParam 直接取字符串 | 2359e97 |
| P0-3 | 严重 | server/src/services/season-pass-service.ts | claimSeasonReward 三处(L182/L198/L209)硬编码 `season_id = 0`，与 getCurrentSeason 用真实 season.id 查询错位，新赛季开始后老赛季奖励被记到 season_id=0 形成跨赛季污染 | 函数开头查询当前赛季 ID（与 getCurrentSeason 一致），三处硬编码 0 替换为查询到的 seasonId | 2359e97 |
| P1-1 | 中等 | server/src/app.ts | gracefulShutdown(L197-199)中 `io?.close()` 和 `httpServer.close()` 未 await，可能导致 WebSocket 连接占用端口引发 httpServer.close 卡住，或端口未释放就退出进程 | 用 Promise 包装 io.close 和 httpServer.close 确保 await | 2359e97 |
| P1-2 | 中等 | server/src/app.ts | L62 `app.use(cors())` 无参数允许所有来源，与 WebSocket 侧 config.corsOrigin 配置不一致，生产环境收紧 CORS_ORIGIN 后 HTTP 仍允许任意来源形成安全绕过 | 改用 `cors({ origin: config.corsOrigin })` 与 WebSocket 配置一致 | 2359e97 |
| P1-3 | 中等 | server/src/routes/settle.ts | L25-35 `durationSeconds` 无任何校验，可传负数、0 或超大值，导致下游 service 按时长计算的奖励倍率失真 | 添加校验：必须是 1-3600 内的有限正数 | 2359e97 |
| P1-5 | 中等 | server/src/websocket/room-manager.ts | L252 关卡生成失败时房间状态恢复 withRoomLock 的 catch 静默吞错 `.catch(() => {})`，房间状态恢复失败时无任何日志，难以排查房间卡死在 generating 的问题 | 改为带 logger.error 的 catch，记录错误与 roomId | 2359e97 |
| 前端-P1 | 中等 | client/src/pages/records.tsx | L44-62 loadRecords 缺少 requestId 守卫，快速翻页时旧请求可能后返回覆盖新请求结果，导致显示与页码错位 | 添加 loadRequestIdRef 守卫，丢弃过时请求结果 | 37844ba |

## 未修复的问题（需评估）

| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| P1-4 | 中等 | server/src/services/user-service.ts L206-214 | refreshToken 黑名单检查 Redis 故障时降级放行（安全风险） | 与 WebSocket 侧握手黑名单降级策略一致（websocket/index.ts L67-71），改成 fail-closed 会导致 Redis 故障时所有用户无法刷新令牌登出，影响范围更大；属设计降级策略，需产品评估后统一调整，不在本次最小修复范围 |

## 记录的轻微问题（P2，未修复）

- 经检查 client/src/pages/battle.tsx L248-259 onLevelReady 双触发：L255 已有 `if (battleSceneRef.current) return;` 守卫处理重连场景，非真实 bug
- 经检查 client/src/pages/battle.tsx room:join 重复 emit：L133 已有 hasJoined 标志位守卫，L149-152 的 onConnect 只在 !hasJoined 时 emit，非真实 bug

## 验证结果

- 修复后 server `tsc --noEmit`：通过
- 修复后 server 单元测试：通过（678/678，含 season-pass-service 14、friend-service 16、leaderboard-service 20、room-manager 40 等相关测试）
- 修复后 server `npm run build`（tsc）：通过
- 修复后 client `npm run lint`：通过
- 修复后 client 单元测试：通过（243/243）
- 修复后 client `npm run build`：通过（dist 产物正常输出）

## 同步更新的测试用例

为保证类型契约修改后测试不破坏，同步更新以下测试文件（已包含在对应提交中）：
- server/src/services/leaderboard-service.test.ts：userId 改为 UUID 字符串
- server/src/services/friend-service.test.ts：所有数字 ID 改为 UUID 字符串（uuid-1/uuid-2/uuid-5/uuid-8/uuid-10/uuid-99 等）
- server/src/routes/friends.test.ts：所有数字 ID 改为 UUID 字符串；移除"无效 ID（非数字）返回 400"用例（因 friendId 不再 parseInt）
- server/src/services/season-pass-service.test.ts：claimSeasonReward 各用例添加当前赛季查询 mock（`{ rows: [{ id: 7 }] }`），断言 season_id 从 0 改为 7

## 根因分析

3 个 P0 问题中有 2 个（P0-1、P0-2）源于 schema 使用 UUID 主键但部分 service/route 代码假设 ID 是 number 类型，且单元测试用数字字符串 `'1'` 作为 userId 掩盖了此问题。建议后续：
1. 在 database schema 与 service 层之间建立类型契约校验（如代码生成或 schema 验证）
2. 单元测试使用更真实的 UUID 字符串 fixture，避免类型不匹配被 mock 掩盖
