# Bug 检查与修复报告 - auto-emotion

## 检查时间
2026-07-28 00:48

## 检查范围
- 前端：lint/test/build 结果
  - `npm run lint`：通过（exit 0）
  - `npm run test`：通过（31 test files / 254 tests passed）
  - `npm run build`：通过（990ms 完成，34 个产物）
- 后端：test/build 结果
  - `npm run test`：通过（56 test files / 731 tests passed）
- 代码变更分析范围：git diff HEAD~7（最近 7 次提交，含 refactor 与 style 提交）
- 前后端状态机一致性检查结果：见下文明细
- 端到端关键路径可达性检查结果：见下文明细
- 前后端数据类型/字段一致性检查结果：见下文明细

## 已修复的问题

| 序号 | 严重程度 | 文件 | 问题描述 | 修复方式 | 提交哈希 |
|---|---|---|---|---|---|
| 1 | P0 严重 | client/src/game/scenes/battle-scene.ts | `onGameOver` 回调为空函数，导致游戏结束后 `game:finish` 事件从未发送，后端无法触发 `playing→settling` CAS 状态转换，结算弹窗永不显示，房间卡死在 `playing` 状态 | 新增 `emitFinish` 方法（含 `finished` 标志防重复 emit），在 boss/brawl/speed 三种模式的 `onGameOver` 回调中分别调用，按胜负语义上报 `result: 'win'/'lose'` 与 `finalScore` | a1813b6 |
| 2 | P0 严重 | client/src/pages/room.test.tsx | 测试 mock 的 `status` 固定为 `'waiting'`，但 room.tsx 已将"开始游戏"按钮显示条件改为 `status === 'ready'`（与后端 `startGame` 前置条件对齐），导致"房主开始游戏确认弹窗"测试组找不到按钮，测试失败 | 引入 `vi.hoisted` 动态 `storeState` 与 `setStatus`，在不同测试组设置匹配的 `status` 值（开始游戏测试组设为 `'ready'`，其余保持 `'waiting'`） | a1813b6 |
| 3 | P1 中等 | client/src/pages/battle.tsx | 多玩家同时 `emit game:finish` 时，后端 CAS 仅首个成功，其余玩家收到 `CONFLICT "游戏未在进行中"` 错误，`onRoomError` 回调用 `setError` 覆盖已显示的结算弹窗，导致用户看到错误提示而非结算结果 | 新增 `settlementRef` 跟踪结算状态，`onRoomError` 中判断 `settlementRef.current.show` 为真时直接 return，忽略结算已显示后的并发冲突错误 | a1813b6 |

> 说明：本次修复在暂存后由并行的自动开发流程一并打包进提交 `a1813b6`（原 refactor: brawl-game 抽取常量），commit 消息虽未体现 fix，但上述三个文件的修复内容均已落入该提交。当前 HEAD 为 `e8899765`，`git diff HEAD` 为空，修复已在主干且与 `origin/main` 同步。

## 未修复的问题（需评估）
| 序号 | 严重程度 | 文件 | 问题描述 | 不修复原因 |
|---|---|---|---|---|
| 无 | - | - | 本次检查未发现需要评估的 P0/P1 问题 | - |

## 记录的轻微问题（P2，未修复）
- **room.tsx 准备按钮 UX**：前端准备按钮显示条件为 `status === 'waiting'`，但后端 `setReady` 接受 `status ∈ {waiting, ready}`。这意味着当所有玩家准备后房间进入 `ready` 状态，非房主玩家将无法通过 UI 取消准备（按钮消失），只能等房主开始或主动离开房间。属 UX 优化范畴，不影响核心流程可达性，记为 P2 不修复。
- 部分文件存在并行开发产生的样式调整（idle.tsx/lobby.tsx/room.tsx/tasks.tsx 工作树有未提交修改），不属于本次 bug 修复范围，未处理。

## 状态机一致性检查明细

后端房间状态机：`waiting → ready → generating → playing → settling → closed`

| 页面 | 按钮/操作 | 前端显示条件 | 后端前置条件 | 是否一致 |
|---|---|---|---|---|
| room.tsx | 设置对战模式 | `isHost && status === 'waiting'` | `hostId === userId && status === 'waiting'` | 一致 |
| room.tsx | 提交压力源 | `status === 'waiting'` | `status === 'waiting'` | 一致 |
| room.tsx | 准备/取消准备 | `status === 'waiting'` | `status ∈ {waiting, ready}` | 前端更严格（P2，见轻微问题） |
| room.tsx | 房主开始游戏 | `isHost && status === 'ready'` | `hostId === userId && status === 'ready'` | 一致 |
| room.tsx | 离开房间 | 始终可点 | `leaveRoom` 无状态守卫 | 一致 |
| battle.tsx | game:finish 上报 | 游戏运行中自动触发 | `status === 'playing'` CAS | 一致（修复后） |
| battle.tsx | game:action 上报 | 本地射击时触发 | 房间内成员 | 一致 |

## 端到端关键路径检查明细

| 路径 | 起始状态 | 目标状态 | 是否可达 | 备注 |
|---|---|---|---|---|
| 创建房间 | (无) | waiting | 可达 | roomStore.createRoom |
| 加入房间 | waiting | waiting | 可达 | socket emit room:join |
| 玩家准备 | waiting | ready | 可达 | 全员 ready 后 status→ready |
| 房主开始游戏 | ready | generating→playing | 可达 | 修复后 onGameOver→emitFinish 闭环 |
| 游戏中操作同步 | playing | playing | 可达 | game:action 广播 + 本地重现 |
| 游戏结束上报 | playing | settling | 可达 | **修复前断裂**（onGameOver 空），修复后 emitFinish 触发 CAS |
| 结算弹窗展示 | settling | settling | 可达 | **修复前被 CONFLICT 错误覆盖**，修复后 settlementRef 过滤 |
| 离开房间 | 任意 | closed | 可达 | leaveRoom |
| 断线重连 | (断线) | 恢复原状态 | 可达 | websocket/index.ts reconnect 处理 rejoin + 补发 level-ready |

## 前后端数据类型/字段一致性检查明细

| 事件/接口 | 方向 | 前端字段 | 后端字段 | 是否一致 |
|---|---|---|---|---|
| `game:finish` (client→server) | emit | `{roomId, finalScore, result}` | `FinishInput {roomId, finalScore, result}` | 一致 |
| `game:finish` (server→client) | on | `{userId, finalScore, result}` | `FinishPayload {userId, finalScore, result, timestamp}` | 一致（前端忽略 timestamp） |
| `game:action` (client→server) | emit | `{roomId, action, payload}` | `ActionInput {roomId, action, payload?}` | 一致 |
| `game:action` (server→client) | on | `{userId, action, payload, timestamp}` | `ActionPayload {userId, action, payload?, timestamp}` | 一致 |
| `game:score-update` (server→client) | on | `{userId, score}` | `ScoreUpdatePayload {userId, score, combo, timestamp}` | 一致（前端忽略 combo/timestamp） |
| `room:state` (server→client) | on | `{room: {players, status}}` | `StatePayload {room: Room}` | 一致 |
| `room:error` (server→client) | on | `{message}` | `ErrorPayload {code?, message}` | 一致（code 可选） |
| `room:join` (client→server) | emit | `{roomId, nickname}` | `JoinInput {roomId, nickname}` | 一致 |

## 验证结果
- 修复后 lint：通过
- 修复后 test：通过（前端 254/254，后端 731/731）
- 修复后 build：通过
